.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local secrets. Never commit.
.env

# Local object storage. Originals and page renders live here under the filesystem
# backend; nothing in it belongs in git. The glob matters: REEF_STORAGE_ROOT is
# configurable, and a benchmark run under a different root once staged the entire
# indexed corpus.
.reef-storage*/

# Build artifacts
dist/
*.egg-info/
